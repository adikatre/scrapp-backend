# Trash Detection with YOLO (TACO, local dataset)

This project trains a YOLOv11 detector on your **local** TACO dataset folder and
provides a script to run detections and return **bin guidance**.

Your dataset is already downloaded; for example:
