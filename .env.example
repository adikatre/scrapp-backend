OPENAI_API_KEY=
USE_YOLO=false
# Optional comma-separated extra origins (e.g. http://localhost:3000 for local dev)
# CORS_ORIGINS=

# Shared secret the frontend must send as `Authorization: Bearer <key>`.
# Generate with: openssl rand -hex 32
BACKEND_API_KEY=
